import React from 'react';
import Fakedata from '../../../Fakedata/data'


const Courses = () => {
    const data = Fakedata;
    console.log(data);
    return (
        <div>
       
        </div>
    );
};

export default Courses;